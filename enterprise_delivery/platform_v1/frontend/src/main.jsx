import React from 'react';
import ReactDOM from 'react-dom/client';
import EnterpriseControlHub from '../EnterpriseControlHub.jsx';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <EnterpriseControlHub />
  </React.StrictMode>,
);
